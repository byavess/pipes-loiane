import { FitroArrayImpuroPipe } from './fitro-array-impuro.pipe';

describe('FitroArrayImpuroPipe', () => {
  it('create an instance', () => {
    const pipe = new FitroArrayImpuroPipe();
    expect(pipe).toBeTruthy();
  });
});
